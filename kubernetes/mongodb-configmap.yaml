apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-configmap
  namespace: epicalendar
data:
  init.sh: |
    #!/bin/bash
    echo "Initializing MongoDB..."
    # Add any initialization scripts here if needed
